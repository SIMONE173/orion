# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# database (demo: si rigenera; cancellare per reset)
/data/*.db
/data/*.db-journal
/data/*.db-wal
/data/*.db-shm

# env
.env
.env.local
.env*.local

# misc
.DS_Store
*.pem
*.log
npm-debug.log*

# backup locali del DB (mai versionare)
/data/*.bak*

# ORION Desktop (Electron)
/desktop/node_modules
/desktop/dist

# Il trailer è un MP4 separato: la route resta solo locale (render), MAI sul sito.
src/app/trailer/
trailer-render/

# Media pesanti / backup: mai su git (rifiutati da GitHub, gonfiano il repo)
*.mp4
*.aiff
*.tgz
/voce/
data/backups/

# Rapporti degli stress test (contengono trascrizioni)
stress/rapporti/

# ── ROBA CHE NON DEVE MAI FINIRE NEL REPOSITORY ──────────────────────────────
# Il 3/8/2026 un «git add -A» ha trascinato dentro una chiave API vera, i video
# della campagna e perfino un node_modules. GitHub ha bloccato il push. Perché
# non possa succedere di nuovo, queste porte restano chiuse per costruzione.
/_recupero/            # archivi di recupero: contengono file di ambiente veri
/campagna-demo/        # video, audio e materiali della campagna (pesanti)
/desktop/dist/         # gli installer costruiti
.carica-installer.mjs  # script di servizio con accesso al deposito
.check-crediti.mjs
.collaudo-demo.ts
PIANO.md
MERCATO.md
tsconfig.tsbuildinfo   # cache di TypeScript: cambia a ogni compilazione
